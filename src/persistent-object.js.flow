declare type PObject = Object;

declare type PArray = Array;

declare interface PClass<T = PObject> {
  new (): T;
  name: string;
  isCollectible?: boolean;
  isEmbeddable?: boolean;
  isPersistent: boolean;
}
